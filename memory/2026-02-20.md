# 2026-02-20

## 🦐 初次对话 - 定义角色与规则

**时间**：北京时间 2026-02-20 18:12 起（UTC 10:12）
**参与者**：老板（董欣）、小虾米（我）

### 关键约定
1. **称谓**：
   - 用户称呼：老板（董欣）
   - 我的名字：小虾米 🦐
   - 关系：AI小助手/老板的跟班

2. **时区**：北京时间（GMT+8）

3. **沟通语言**：中文

4. **汇报风格**：
   - 详细式汇报
   - **先给结论** → **具体步骤** → **查证过程与理由**
   - 严禁“Great question!”等填充词，直接帮助

5. **核心原则（老板特别强调）**：
   > 知之为知之，不知为不知；不知道的东西要先去查询和确认。如果查询和确认以后还是不清楚/不确定，那就告诉我你不知道/不确定，任何时候都不可以胡编乱造，所有任务中不可以使用模拟数据，随机函数，或者杜撰的虚拟信息，不能出现AI幻觉。
   
   **已更新到 SOUL.md**：加入“Honesty & Accuracy”条款，强调真实性优先于完成度。

### 技术信息
- **当前通道**：Feishu（已配置）
- **邮件功能状态**：
  - OpenClaw **无内置邮件发送通道**
  - 支持 **Gmail Pub/Sub** 接收邮件通知（需额外配置）
  - 发送邮件需通过外部脚本/cron或第三方服务集成

### 文件更新记录
- `IDENTITY.md`：创建，定义“小虾米”身份
- `USER.md`：创建，记录老板信息
- `SOUL.md`：更新，加入真实性原则
- `BOOTSTRAP.md`：已删除（初始化完成）

---

**记忆重点**：老板重视准确性、真实性；厌恶AI幻觉；喜欢详细、结构化的汇报方式。

## 📅 会议安排记录

### 2026-02-21（明天）
- **会议室**：302房间
- **记录时间**：2026-02-20 20:08（UTC 12:08）通过飞书告知

## 📧 邮箱配置与邮件发送记录

### 2026-02-20 20:28
- **邮箱配置**：
  - 邮箱地址：jamesdong2008@gmail.com（老板为小虾米注册）
  - 使用 Gmail SMTP：smtp.gmail.com:587
  - 配置方式：创建 `.env` 文件于 `imap-smtp-email` 技能目录
- **邮件发送**：
  - 收件人：12127083@qq.com（老板邮箱）
  - 主题：中国过年拜年传统
  - 内容：基于 tavily-search 搜索整理的拜年传统（主要习俗、地域差异等）
  - 发送结果：成功 ✅ (邮件ID: <251d255f-18de-1007-3fb0-e308f9243eb9@gmail.com>)
- **安全提醒**：已提醒老板删除飞书中包含 App Password 的消息。

**技能依赖**：`imap-smtp-email` 技能依赖 `nodemailer` 等包，已通过 `npm install` 安装完成。

## 🔍 网页总结规范确定

**时间**：2026-02-20 23:45（UTC 15:45）

**决策**：
- **方案选择**：采用 **方案A**（`summarize` 技能自动化总结 + 二次整理）
- **详细度**：`--length xxl` + `--max-output-tokens 8000`（最大值）
- **输出结构**：
  1. 结论（整体定性）
  2. 关键信息（分点/表格）
  3. 查证过程（工具、验证、不确定性说明）

**技术配置**：
- DeepSeek API 密钥已配置（环境变量）
- `summarize` 技能已安装且可正常调用
- 偏好已记录于 `TOOLS.md` 中，作为后续执行标准

## 📈 T99: A股短线扫描定时任务

**任务英文名**：`a_share_intraday_scan`

**创建时间**：2026-02-20 23:52（UTC 15:52）

**目标**：为 `a-share-short-decision` 技能设置每日14:30的定时扫描，并将结果推送至飞书群。

**已完成配置**：
1. **依赖安装**：已安装 `akshare`、`pandas`（通过 `python3-pip`）
2. **策略调整**：将 `config.json` 中的 `screener` 参数改为**保守模板**（`prefilter_change_pct: 6.0`, `min_change_pct: 7.0` 等）
3. **脚本编写**：
   - `run_scan.sh`：执行扫描并发送至飞书群 `chat:oc_ff08c55a23630937869cd222dad0bf14`
   - `setup_cron.sh`：添加系统 crontab 任务（周一至周五 14:30）
   - `remove_cron.sh`：移除任务
4. **日志记录**：扫描输出将记录至 `scan.log`

**待测试**：运行 `./run_scan.sh` 验证功能（建议先添加 `[测试]` 标记）。

**后续更新**：如需修改此任务，请引用 **T99** 及具体更新内容。

### 2026-02-21 00:42（UTC 16:42）决策更新
- **优化方案**：采用 **选项C**（全自动化优化闭环）
- **实施计划**：
  1. **数据积累**：运行定时任务（周一至周五 14:30）积累至少两周预测数据
  2. **首次复盘**：两周后自动运行复盘脚本，计算胜率、盈亏比等指标
  3. **策略迭代**：根据复盘结果自动调整 `config.json` 参数，或通过机器学习模型优化选股规则
- **技术调整**：
  - 扫描超时从 120 秒改为 **300 秒**（适应较慢的数据源）
  - 定时任务已部署（`crontab` 条目验证成功）
- **自动化流程**：
  - 每日扫描 → 记录预测日志（`data/decision_log.jsonl`）
  - 定期复盘 → 生成绩效报告 → 调整策略 → 重新部署
  - 循环持续，无需人工干预（仅监控异常）

**下一步**：监控首次运行（今日 14:30），确保日志正常记录；两周后触发首次复盘。

### 2026-02-21 01:00（UTC 17:00）优化方案 B + C 实施
- **用户指令**：结合方案B（板块优先扫描 + 交易日历）与方案C（全自动化优化闭环），不改变扫描时间（保持14:30）。
- **已完成的优化**：
  1. **交易日历集成**：`run_scan.sh` 开头添加智能交易日历检查（akshare + 星期几回退），非交易日自动跳过扫描。
  2. **板块早期筛选**：修改 `tools/market_data.py` 中的 `scan_strong_stocks` 函数，在实时模式下，若指定板块列表，则在初筛阶段即过滤股票所属行业，大幅减少候选池（`universe` 从120缩至80）。
  3. **全自动化闭环增强**：
     - 创建 `auto_tune.py` 脚本，可根据复盘日志自动调整 `config.json` 参数（基于胜率、收益率、样本量）。
     - 修改 `run_review.sh`，在复盘结束后自动运行 `auto_tune.py`，实现“复盘→调参”闭环。
     - 调参策略：胜率 <45% 则收紧阈值，胜率 >55% 且样本不足则放宽阈值，收益率 <0.5% 则提高量比要求。
- **待进一步优化（后续迭代）**：
  - 真正的“板块优先”扫描：直接调用 `ak.stock_board_concept_spot_em` 等接口，避免拉取全市场行情。
  - 机器学习模型集成：使用历史特征训练简单分类器，替代部分规则筛选。
  - 多策略赛马：并行运行保守/平衡/激进三个配置，定期选择最优者。
- **今日首次运行**：14:30 将执行优化后的扫描（带板块筛选、交易日历检查），预计扫描时间略有缩短，候选股票更聚焦热点板块。

### 2026-02-21 01:10（UTC 17:10）复盘调度
- **首次复盘日期**：改为 **2026‑03‑14**（用户指定）。
- **执行方式**：已通过 `at` 命令调度一次性任务（作业ID 1），当日 15:00 自动运行 `run_review.sh` 并发送报告至飞书群。
- **脚本更新**：`run_review.sh` 已启用自动发送，并增加“使用最早日志日期”逻辑。
- **详见**：`memory/2026‑02‑21.md`。