# T01飞书推送修复计划

**日期**: 2026-03-13  
**任务**: 修复T01调度器飞书消息推送问题  
**方案**: 使用OpenClaw Message Tool替代openclaw CLI  

---

## 问题概述

当前T01 scheduler使用subprocess调用openclaw CLI发送飞书消息，但在cron环境中因PATH问题导致`node: No such file or directory`错误。

**目标**: 改用OpenClaw内置的`message`工具，直接发送消息到当前飞书对话。

---

## 实施计划

### 任务1: 分析现有消息发送代码
**时间**: 2-3分钟  
**目标**: 理解当前scheduler.py中消息发送逻辑  

**步骤**:
1. 阅读scheduler.py中`send_feishu_message`方法
2. 阅读`_send_feishu_message_optimized`方法
3. 识别消息格式和内容
4. 理解消息触发时机（T日评分后、竞价分析后）

**测试验证**: 无（代码阅读任务）

---

### 任务2: 创建新的消息发送模块
**时间**: 5-7分钟  
**目标**: 创建直接使用message tool的新发送方法  

**步骤**:
1. 在scheduler.py中添加新的发送方法`send_feishu_message_direct`
2. 导入message工具
3. 实现直接调用message(action="send", channel="feishu", message=...)
4. 保持与现有消息格式兼容
5. 添加错误处理和日志

**TDD步骤**:
- [ ] 编写测试：验证新方法可以正确格式化消息
- [ ] 运行测试：应失败（方法未实现）
- [ ] 实现方法
- [ ] 运行测试：应通过

---

### 任务3: 替换现有发送调用
**时间**: 3-5分钟  
**目标**: 将scheduler中所有openclaw CLI调用替换为新方法  

**步骤**:
1. 找到所有调用`send_feishu_message`的地方
2. 修改`send_feishu_message`方法内部逻辑，使用新方法
3. 移除对openclaw CLI的依赖
4. 确保消息内容格式不变

**修改点**:
- T日评分完成后的推送
- T+1竞价分析完成后的推送
- 其他状态通知

**TDD步骤**:
- [ ] 编写测试：验证消息发送调用正确
- [ ] 运行测试：应失败
- [ ] 修改调用点
- [ ] 运行测试：应通过

---

### 任务4: 测试消息发送功能
**时间**: 5-7分钟  
**目标**: 验证新发送方法能正常工作  

**步骤**:
1. 创建测试脚本，模拟scheduler发送消息
2. 发送测试消息到当前对话
3. 验证消息格式正确
4. 测试错误处理（如网络问题）

**TDD步骤**:
- [ ] 编写测试：验证消息能成功发送
- [ ] 运行测试：应通过
- [ ] 测试边界情况：长消息、特殊字符
- [ ] 所有测试通过

---

### 任务5: 部署并重启调度器
**时间**: 3-5分钟  
**目标**: 部署修改后的代码并验证运行  

**步骤**:
1. 备份原scheduler.py
2. 部署修改后的scheduler.py
3. 重启T01调度器服务
4. 验证调度器启动正常
5. 检查日志无错误

**验证**:
- [ ] 调度器进程运行正常
- [ ] 日志显示使用新消息发送方法
- [ ] 等待下次任务触发，验证消息能成功发送

---

## 技术细节

### 当前消息发送代码
```python
# 当前使用openclaw CLI
cmd = [
    openclaw_path, 'message', 'send',
    '--channel', 'feishu',
    '--target', 'user:ou_b8a256a9cb526db6c196cb438d6893a6',
    '--message', message
]
subprocess.run(cmd, ...)
```

### 新消息发送代码
```python
# 使用OpenClaw message tool
from message import message

message(
    action="send",
    channel="feishu",
    message=message_content
)
```

---

## 回滚计划

如果修改后出现问题：
1. 停止调度器：`systemctl stop t01-scheduler`
2. 恢复备份：`cp scheduler.py.bak scheduler.py`
3. 重启调度器：`systemctl start t01-scheduler`

---

## 成功标准

- [ ] T日评分任务完成后，消息能立即发送到飞书
- [ ] T+1竞价分析完成后，消息能立即发送到飞书
- [ ] 无需依赖openclaw CLI和Node.js PATH
- [ ] 消息格式与之前一致
