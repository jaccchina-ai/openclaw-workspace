# T01 龙头战法选股系统 - 详细使用指南

## 📖 文档版本
- **版本**: 1.1.0
- **更新日期**: 2026-02-23
- **适用系统**: T01龙头战法涨停股评分系统
- **目标用户**: 系统管理员、策略研究员、交易员

## 🎯 系统概述

T01系统是一个基于龙头战法的自动化涨停股评分系统，专为A股短线交易设计。系统通过tushare API获取东方财富(DC)数据源，实现对涨停股票的多维度评分，并在竞价阶段提供买入推荐。

### 核心特性

✅ **多维度评分系统** - 涨停时间、封单质量、流动性、资金流、热点板块、龙虎榜六大维度
✅ **T日/T+1双阶段流程** - 晚上8点评分 → 次日竞价分析 → 开盘前推送
✅ **实时竞价数据集成** - 严格遵循交易时间逻辑 (9:25-9:29只允许实时数据)
✅ **融资融券风控** - 四大风险因子实时监控
✅ **精准股票过滤** - 100%准确ST过滤、北交所/科创板自动排除
✅ **热点板块判断** - 基于4项阈值标准的智能识别
✅ **自动化调度** - 交易日自动运行，非交易日智能跳过
✅ **飞书消息推送** - 完整指标透明化展示

## 🏗️ 系统架构

### 文件结构
```
tasks/T01/
├── USER_GUIDE.md                 # 本文档
├── README.md                     # 基础介绍
├── api_questions.md              # API接口问题记录
├── config.yaml                   # 主配置文件
├── config.example.yaml           # 示例配置
├── main.py                       # 主程序入口
├── scheduler.py                  # 定时任务调度器
├── limit_up_strategy_new.py      # 核心策略引擎 (V2)
├── api_client.py                 # API客户端 (精简版)
├── output_formatter.py           # 输出格式化器
├── requirements.txt              # Python依赖
├── run.sh                        # 便捷运行脚本
├── manual_t_day_scoring.py       # 手动T日评分工具
├── quick_t_day_scoring.py        # 快速T日评分工具
├── test_message_format.py        # 消息格式测试工具
├── state/                        # 状态文件目录
│   ├── candidates_20260213_to_20260224.json  # 候选股票
│   └── message_example.txt       # 消息格式示例
├── logs/                         # 日志目录
└── output/                       # 输出目录
```

### 数据流程
```
1. T日20:00: 获取涨停股票 → 六维度评分 → 选出前5名候选
2. T+1日09:25: 获取实时竞价数据 → 竞价评分 → 综合评分
3. T+1日09:30前: 风控评估 → 仓位建议 → 飞书推送
```

## ⚙️ 安装与配置

### 1. 环境要求
```bash
# Python版本
python >= 3.8

# 核心依赖
pandas >= 1.5.0
numpy >= 1.21.0
tushare >= 1.2.89
PyYAML >= 6.0
schedule >= 1.2.0
```

### 2. 快速安装
```bash
# 进入T01目录
cd /root/.openclaw/workspace/tasks/T01

# 安装依赖
pip install -r requirements.txt

# 验证tushare token配置
python -c "import tushare as ts; ts.set_token('870008d508d2b0e57ecf2ccc586c23c4ecc37522f5e93890fb3d56ab'); print('Token配置正确')"
```

### 3. 配置文件详解

编辑 `config.yaml` 调整策略参数：

```yaml
# API配置
api:
  endpoint: "http://api.tushare.pro"
  api_key: "870008d508d2b0e57ecf2ccc586c23c4ecc37522f5e93890fb3d56ab"
  timeout: 30

# 数据源配置 (强制使用东方财富DC数据)
data_sources:
  tushare:
    token: "870008d508d2b0e57ecf2ccc586c23c4ecc37522f5e93890fb3d56ab"
    data_source: "DC"
    enabled: true
  akshare:
    enabled: false

# T日评分权重 (总分100分)
strategy:
  t_day_scoring:
    first_limit_time: 30           # 涨停时间 (越早越好)
    order_amount_to_circ_mv: 15    # 封单金额/流通市值
    buy_to_sell_ratio: 10          # 封成比
    turnover_rate_to_20ma: 10      # 换手率/20日均值
    volume_ratio: 5                # 量比
    turnover_rate: 5               # 换手率
    main_net_amount: 5             # 主力净额
    main_net_ratio: 5              # 主力净占比
    medium_net_amount: 5           # 中单净额
    is_hot_sector: 10              # 热点板块

# T+1竞价评分权重
  t1_auction_scoring:
    open_change_pct: 40            # 开盘涨幅 (40分)
    auction_volume_ratio: 20       # 竞价量比
    auction_turnover_rate: 20      # 竞价换手率
    auction_amount: 20             # 竞价金额

# 风控参数
  risk_control:
    index_5ma_relation: true
    max_position_per_stock: 0.2    # 单只股票最大仓位20%
    min_total_score: 60            # 最低总分要求

# 热点板块阈值 (老板确认)
hot_sector_thresholds:
  sector_gain: 3.0                 # 板块涨幅 ≥ 3%
  main_net_inflow: 50000000        # 主力净流入 ≥ 5000万元
  limit_up_stocks: 3               # 板块内涨停个股 ≥ 3只
  sector_rank: 10                  # 板块排名前10
```

## 🚀 使用方法

### 1. 手动运行T日评分
```bash
# 基于2月13日数据生成候选股票
cd tasks/T01
python3 quick_t_day_scoring.py

# 详细版本 (显示更多信息)
python3 manual_t_day_scoring.py
```

### 2. 主程序多种模式
```bash
# T日评分模式
python3 main.py t-day --date 20260213

# T+1竞价分析模式
python3 main.py t1-auction --date 20260224 --candidates state/candidates_20260213_to_20260224.json

# 完整流程模式
python3 main.py full-pipeline --t-date 20260213 --t1-date 20260224

# 测试API连接
python3 main.py test-api

# 查看配置
python3 main.py config

# 快速运行 (使用最近交易日)
python3 main.py run
```

### 3. 定时任务调度器
```bash
# 启动调度器
python3 scheduler.py

# 模拟运行完整流程
python3 scheduler.py --mode test

# 手动触发T日任务
python3 scheduler.py --run-t-day --date 20260213

# 手动触发T+1任务
python3 scheduler.py --run-t1-auction --date 20260224 --t-date 20260213
```

### 4. 消息格式测试
```bash
# 测试优化后的消息格式
python3 test_message_format.py
```

## 📊 核心API接口说明

### 已集成并测试的tushare接口

| 接口名称 | 数据类别 | 状态 | 关键字段 | 用途 |
|---------|----------|------|----------|------|
| `limit_list_d` | 涨停股数据 | ✅ | `first_time`, `fd_amount`, `amount`, `float_mv` | 涨停时间、封单金额、成交额 |
| `moneyflow_dc` | 个股资金流 | ✅ | `buy_sm_vol`, `sell_sm_vol`, `buy_med_vol` | 主力、中单资金流向 |
| `moneyflow_ind_dc` | 板块资金流 | ✅ | `name`, `net_mf_vol`, `net_mf_amount` | 行业板块资金流向 |
| `dc_daily` | 板块行情 | ✅ | `pct_chg`, `amount` | 板块涨跌幅、成交量 |
| `stk_auction_o` | 历史竞价 | ✅ | `open`, `vol`, `amount` | 开盘集合竞价历史数据 |
| `stk_auction` | 实时竞价 | ✅ | `price`, `pre_close`, `volume_ratio` | 实时竞价数据 (9:25-9:29) |
| `stock_st` | ST列表 | ✅ | `ts_code`, `name` | 官方ST股票过滤 |
| `daily_basic` | 技术指标 | ✅ | `volume_ratio`, `turnover_rate_f` | 量比、换手率 |
| `margin` | 融资融券 | ✅ | `fin_balance`, `sec_balance` | 融资余额、融券余额 |
| `trade_cal` | 交易日历 | ✅ | `cal_date`, `is_open` | 交易日判断 |

### 关键数据字段解释

**涨停数据 (`limit_list_d`)**:
- `first_time`: 首次涨停时间 (格式: HHMMSS, 如: 132036 → 13:20:36)
- `fd_amount`: 封单金额 (单位: 元)
- `amount`: 成交金额 (单位: 元)
- `float_mv`: 流通市值 (单位: 元)

**竞价数据 (`stk_auction`)**:
- `price`: 开盘价 (9:25确定)
- `pre_close`: 前收盘价
- `volume_ratio`: 竞价量比 (与过去5日均量比较)

**资金流数据 (`moneyflow_dc`)**:
- `buy_sm_vol`: 主力买入成交量
- `sell_sm_vol`: 主力卖出成交量
- `buy_med_vol`: 中单买入成交量

## 🔄 2月24日实时测试流程

### 测试准备
```bash
# 1. 生成候选股票 (已执行)
cd tasks/T01
python3 quick_t_day_scoring.py

# 2. 验证候选文件
ls -la state/candidates_20260213_to_20260224.json

# 3. 检查消息格式
python3 test_message_format.py
```

### 实时测试时间表
```
⏰ 09:25-09:29: stk_auction实时接口首次实测
📊 实时评分: 基于候选股票 + 竞价数据
📱 09:30前: 推送包含所有指标数值的最终推荐
```

### 测试脚本
```bash
# 手动模拟实时测试 (需在交易时间)
cd tasks/T01
python3 scheduler.py --run-t1-auction --date 20260224 --t-date 20260213

# 或使用主程序
python3 main.py t1-auction --date 20260224 --candidates state/candidates_20260213_to_20260224.json
```

### 预期输出
1. ✅ 实时竞价数据获取成功
2. ✅ 融资融券风控数据获取成功
3. ✅ 综合评分计算完成
4. ✅ 飞书消息格式正确生成
5. ✅ 所有指标数值完整显示

## 🛠️ 故障排除

### 常见问题及解决方案

#### 1. API返回空数据
**现象**: `limit_list_d`等接口返回空DataFrame
**原因**: 使用了非交易日日期
**解决**: 
```python
# 检查交易日
python3 -c "import tushare as ts; ts.set_token('YOUR_TOKEN'); pro = ts.pro_api(); cal = pro.trade_cal(exchange='SSE', start_date='20260213', end_date='20260213'); print('是否交易日:', cal.iloc[0]['is_open'] if not cal.empty else '无数据')"
```

#### 2. 实时竞价数据获取失败
**现象**: 9:25-9:29期间无法获取`stk_auction`数据
**原因**: 非交易时间或接口异常
**解决**: 
- 系统会自动报错"无法选股"（符合设计要求）
- 检查网络连接和tushare权限
- 验证是否在正确的交易时间段

#### 3. ST股票过滤异常
**现象**: ST股票未被正确过滤
**解决**: 
- 检查`stock_st`接口是否正常工作
- 验证过滤规则：`code.startswith('8')` (北交所)、`code.startswith('688')` (科创板)

#### 4. 评分结果异常
**现象**: 所有股票评分均为0或极低
**原因**: 数据字段获取失败
**解决**: 
- 检查API返回字段是否完整
- 查看`logs/t01_scheduler.log`日志
- 验证各评分因子是否正常计算

### 日志检查
```bash
# 查看调度器日志
tail -f logs/t01_scheduler.log

# 查看策略日志
tail -f t01_limit_up.log

# 查看错误日志
grep -i "error\|exception\|failed" logs/t01_scheduler.log
```

### 性能监控
```bash
# API调用统计
grep "API调用" logs/t01_scheduler.log | tail -10

# 评分时间统计
grep "成功评分" logs/t01_scheduler.log | tail -5

# 内存使用情况
ps aux | grep python | grep T01
```

## 📋 消息推送格式说明

### 消息结构
```
📊 **T01策略推荐 - {交易日期}**
==========================================
**市场状况**: {风险等级描述}
**风险等级**: {低/中/高/极高}
**建议**: {操作建议}
**融资余额**: {X.XX}万亿元
**融券余额**: {X.XX}亿元
==========================================
**🎯 推荐股票 (X只)**

#1 **股票名称** (股票代码)
  评分: {总分} (T日: {T日分}, 竞价: {竞价分})
  操作: {买入/观望} | 仓位: {X.X}% | 置信度: {高/中/低}
  理由: {理由1, 理由2, ...}
  **竞价指标**:
    开盘涨幅: {+X.XX}%
    竞价量比: {X.XX}
    竞价金额: {X.XX}万
  **涨停指标**:
    涨停涨幅: {X.XX}%
    首次涨停: {HH:MM:SS}
    封成比: {X.XXX}
    封单/流通: {X.XX}bp
    换手率: {X.XX}%
    热点板块: {是/否}
  **资金信息**:
    封单金额: {X.XX}万
    成交金额: {X.XX}万
    流通市值: {X.XX}亿
==========================================
**📋 注意事项**
1. 设置止损位（建议-6%）
2. 关注大盘走势变化
3. 严格执行仓位控制
```

### 消息长度控制
- ✅ **字符数**: 通常在1200-1800字符之间
- ✅ **行数**: 80-120行
- ✅ **飞书兼容**: 完全兼容飞书消息格式

## 🔧 系统维护

### 日常维护
```bash
# 清理旧日志 (保留最近7天)
find logs/ -name "*.log" -mtime +7 -delete

# 清理旧输出文件 (保留最近30天)
find output/ -name "results_*.csv" -mtime +30 -delete

# 清理状态文件 (除候选文件外)
find state/ -name "*.json" ! -name "*candidates*" -mtime +7 -delete
```

### 配置更新
```bash
# 更新策略参数后同步Registry
cd /root/.openclaw/workspace
python3 sync_registry.py sync --task T01

# 验证配置
python3 sync_registry.py check --task T01
```

### 版本升级
1. 备份当前配置和状态文件
2. 更新代码
3. 更新`config.yaml`版本号
4. 运行同步脚本
5. 执行回归测试

## 📞 技术支持

### 紧急联系方式
- **系统状态**: 查看 `openclaw status`
- **日志位置**: `tasks/T01/logs/`
- **配置备份**: `tasks/T01/config.yaml.bak`

### 调试模式
```bash
# 启用详细日志
python3 main.py t-day --date 20260213 --verbose

# 调度器调试模式
export LOG_LEVEL=DEBUG
python3 scheduler.py
```

### 数据验证工具
```bash
# 验证交易日数据
cd tasks/T01
python3 quick_trading_day_test.py

# 验证API接口
python3 test_new_apis.py

# 验证风控模块
python3 test_margin_risk.py
```

## 📚 附录

### A. 配置文件完整示例
详见 `config.example.yaml`

### B. 候选文件格式
详见 `state/candidates_20260213_to_20260224.json`

### C. Task Registry同步规则
系统自动通过Git钩子同步版本信息：
- 修改`config.yaml`后自动更新Task Registry
- Registry版本必须与代码版本一致
- 聊天内容与Registry冲突时以Registry为准

### D. 许可证信息
- 系统代码: 开源协议
- tushare数据: 商业授权
- 东方财富数据: API服务条款

---

**文档版本控制**
| 版本 | 日期 | 更新内容 | 更新人 |
|------|------|----------|--------|
| 1.0.0 | 2026-02-22 | 初始版本 | 小虾米 |
| 1.1.0 | 2026-02-23 | 详细使用指南 | 小虾米 |

**最后更新**: 2026-02-23
**文档状态**: ✅ 完成